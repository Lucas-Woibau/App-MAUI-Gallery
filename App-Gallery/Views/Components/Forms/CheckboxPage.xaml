<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="App_Gallery.Views.Components.Forms.CheckboxPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="CheckboxPage">
    <VerticalStackLayout>
        <Label
            x:Name="LblStatus"
            HorizontalOptions="Center"
            Text="Qual a linguagem que você mais gosta?"
            VerticalOptions="Center" />
        <HorizontalStackLayout>
            <CheckBox
                CheckedChanged="CheckBox_CheckedChanged"
                IsChecked="False"
                Color="LightBlue" />
            <Label Text="PHP" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox
                CheckedChanged="CheckBox_CheckedChanged"
                IsChecked="False"
                Color="Red" />
            <Label Text="Java" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox
                CheckedChanged="CheckBox_CheckedChanged"
                IsChecked="False"
                Color="blue" />
            <Label Text="C#" VerticalOptions="Center" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>